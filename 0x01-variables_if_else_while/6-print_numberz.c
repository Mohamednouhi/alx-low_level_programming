#include <stdio.h>
#include <stdlin.h>
#include <time.h>

/**
 * main - numberz
 * Return: always 0 (success)
 */

int main(void)

{
	int d;

	for (d = '0'; d <= '9'; d++)
	putchar(d);
	putchar('\n');

	return (0);
}
